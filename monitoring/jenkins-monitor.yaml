apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: jenkins-monitor
  namespace: observability # חשוב: חייב להיות ב-namespace של הניטור
  labels:
    release: kube-prometheus-stack # התגית שגורמת לפרומתיאוס לזהות את הקובץ
spec:
  selector:
    matchLabels:
      app.kubernetes.io/instance: jenkins
      app.kubernetes.io/name: jenkins
  namespaceSelector:
    matchNames:
      - jenkins
  endpoints:
    - port: http
      path: /prometheus/
      interval: 30s